[project]
name = "powerbi-mcp"
version = "0.2.0"
description = "REST-only, read-only Power BI MCP Server with type-safe Pydantic models"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=2.0.0",
    "msal>=1.24.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0.0",
    "requests>=2.31.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.15.0",
]
